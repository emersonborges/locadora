SELECT
     *,
     Automovel.`nome` AS Automovel_nome,
     Automovel.`ano` AS Automovel_ano,
     Automovel.`cor` AS Automovel_cor,
     Automovel.`preco` AS Automovel_preco,
     Automovel.`status` AS Automovel_status
FROM
     `Automovel` Automovel